{
  "name": "strategic-brief-builder",
  "version": "1.0.0",
  "description": "Web tool to build strategic briefs using AI.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build index.html --dist-dir public --no-cache"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "buffer": "^6.0.3",
    "docx": "^9.3.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "parcel": "^2.14.4"
  }
}
