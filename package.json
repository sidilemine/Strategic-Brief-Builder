{
  "name": "strategic-brief-builder",
  "version": "1.0.0",
  "description": "Web tool to build strategic briefs using AI.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild script.js --bundle --outfile=bundle.js --format=iife --minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "docx": "^9.3.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
